# Handles TradingView webhook alerts
def process_signal(signal):
    print(f"📩 Signal received: {signal}")
    # Yahan aage trade logic aayega
    # e.g., AI backtest, SL/TP optimize, order fire, etc.
